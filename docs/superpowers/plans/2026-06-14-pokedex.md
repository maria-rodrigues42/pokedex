# Pokédex Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Criar do zero uma Pokédex em React + TypeScript com busca por nome via PokeAPI, visual simples e código limpo sem cara de gerado por IA.

**Architecture:** Componentes pequenos com responsabilidade única (SearchBar, PokemonCard, TypeBadge), um hook customizado para chamada de API, e tipos TypeScript centralizados. Sem biblioteca de UI — CSS puro com variáveis.

**Tech Stack:** React 18, TypeScript, Vite, CSS puro, PokeAPI (`https://pokeapi.co/api/v2/pokemon/`)

---

### Task 1: Scaffolding do projeto

**Files:**
- Create: `package.json`, `vite.config.ts`, `tsconfig.json`, `index.html`, `src/main.tsx`

- [ ] **Step 1: Criar projeto com Vite**

```bash
cd /home/maria-rodrigues/Documents/ESTUDO/pokemon
npm create vite@latest . -- --template react-ts
```

Quando perguntar se quer sobrescrever, confirmar com `y`.

- [ ] **Step 2: Instalar dependências**

```bash
npm install
```

- [ ] **Step 3: Remover arquivos desnecessários do template**

```bash
rm -f src/assets/react.svg src/assets/vite.svg public/vite.svg src/App.css
```

- [ ] **Step 4: Limpar `src/App.tsx`**

Substituir conteúdo por:

```tsx
export default function App() {
  return <div>ok</div>
}
```

- [ ] **Step 5: Limpar `src/index.css`**

Substituir conteúdo por (vazio por enquanto):

```css
/* estilos globais virão aqui */
```

- [ ] **Step 6: Verificar que sobe sem erro**

```bash
npm run dev
```

Esperado: servidor rodando em `http://localhost:5173`, página mostra "ok".

- [ ] **Step 7: Init git e primeiro commit**

```bash
git init
git add .
git commit -m "chore: projeto vite criado"
```

---

### Task 2: Tipos TypeScript

**Files:**
- Create: `src/types/pokemon.ts`

- [ ] **Step 1: Criar arquivo de tipos**

Criar `src/types/pokemon.ts` com:

```ts
export interface PokemonType {
  type: {
    name: string
  }
}

export interface Pokemon {
  id: number
  name: string
  height: number
  weight: number
  sprites: {
    front_default: string | null
  }
  types: PokemonType[]
}
```

- [ ] **Step 2: Commit**

```bash
git add src/types/pokemon.ts
git commit -m "feat: tipos do pokemon"
```

---

### Task 3: Hook `usePokemon`

**Files:**
- Create: `src/hooks/usePokemon.ts`

- [ ] **Step 1: Criar hook**

Criar `src/hooks/usePokemon.ts` com:

```ts
import { useEffect, useState } from 'react'
import type { Pokemon } from '../types/pokemon'

interface Estado {
  data: Pokemon | null
  erro: string | null
  carregando: boolean
}

export function usePokemon(query: string): Estado {
  const [data, setData] = useState<Pokemon | null>(null)
  const [erro, setErro] = useState<string | null>(null)
  const [carregando, setCarregando] = useState(false)

  useEffect(() => {
    if (!query.trim()) return

    setCarregando(true)
    setErro(null)

    fetch(`https://pokeapi.co/api/v2/pokemon/${query.toLowerCase()}`)
      .then((res) => {
        if (!res.ok) throw new Error('Pokémon não encontrado.')
        return res.json()
      })
      .then((json: Pokemon) => setData(json))
      .catch((e: Error) => {
        setErro(e.message)
        setData(null)
      })
      .finally(() => setCarregando(false))
  }, [query])

  return { data, erro, carregando }
}
```

- [ ] **Step 2: Commit**

```bash
git add src/hooks/usePokemon.ts
git commit -m "feat: hook usePokemon"
```

---

### Task 4: Componente `TypeBadge`

**Files:**
- Create: `src/components/TypeBadge.tsx`

- [ ] **Step 1: Criar componente**

Criar `src/components/TypeBadge.tsx` com:

```tsx
const cores: Record<string, string> = {
  fire: '#f97316',
  water: '#3b82f6',
  grass: '#22c55e',
  electric: '#eab308',
  psychic: '#ec4899',
  ice: '#67e8f9',
  dragon: '#7c3aed',
  dark: '#374151',
  normal: '#9ca3af',
  poison: '#a855f7',
  ground: '#d97706',
  rock: '#78716c',
  bug: '#84cc16',
  ghost: '#6d28d9',
  steel: '#64748b',
  fighting: '#dc2626',
  flying: '#38bdf8',
  fairy: '#f472b6',
}

interface Props {
  tipo: string
}

export function TypeBadge({ tipo }: Props) {
  const cor = cores[tipo] ?? '#9ca3af'

  return (
    <span
      style={{
        backgroundColor: cor,
        color: '#fff',
        padding: '2px 10px',
        borderRadius: '999px',
        fontSize: '0.75rem',
        fontWeight: 600,
        textTransform: 'capitalize',
        letterSpacing: '0.03em',
      }}
    >
      {tipo}
    </span>
  )
}
```

- [ ] **Step 2: Commit**

```bash
git add src/components/TypeBadge.tsx
git commit -m "feat: componente TypeBadge"
```

---

### Task 5: Componente `SearchBar`

**Files:**
- Create: `src/components/SearchBar.tsx`

- [ ] **Step 1: Criar componente**

Criar `src/components/SearchBar.tsx` com:

```tsx
import { useState } from 'react'

interface Props {
  onBuscar: (nome: string) => void
}

export function SearchBar({ onBuscar }: Props) {
  const [valor, setValor] = useState('')

  function handleSubmit(e: React.FormEvent) {
    e.preventDefault()
    if (valor.trim()) onBuscar(valor.trim())
  }

  return (
    <form className="search-form" onSubmit={handleSubmit}>
      <input
        className="search-input"
        type="text"
        placeholder="nome do pokémon"
        value={valor}
        onChange={(e) => setValor(e.target.value)}
      />
      <button className="search-btn" type="submit">
        buscar
      </button>
    </form>
  )
}
```

- [ ] **Step 2: Commit**

```bash
git add src/components/SearchBar.tsx
git commit -m "feat: componente SearchBar"
```

---

### Task 6: Componente `PokemonCard`

**Files:**
- Create: `src/components/PokemonCard.tsx`

- [ ] **Step 1: Criar componente**

Criar `src/components/PokemonCard.tsx` com:

```tsx
import type { Pokemon } from '../types/pokemon'
import { TypeBadge } from './TypeBadge'

interface Props {
  pokemon: Pokemon
}

export function PokemonCard({ pokemon }: Props) {
  return (
    <div className="pokemon-card">
      {pokemon.sprites.front_default && (
        <img
          className="pokemon-sprite"
          src={pokemon.sprites.front_default}
          alt={pokemon.name}
        />
      )}

      <h2 className="pokemon-nome">{pokemon.name}</h2>

      <div className="pokemon-tipos">
        {pokemon.types.map(({ type }) => (
          <TypeBadge key={type.name} tipo={type.name} />
        ))}
      </div>

      <div className="pokemon-stats">
        <span>{pokemon.height * 10} cm</span>
        <span>{pokemon.weight / 10} kg</span>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Commit**

```bash
git add src/components/PokemonCard.tsx
git commit -m "feat: componente PokemonCard"
```

---

### Task 7: `App.tsx` e estilos globais

**Files:**
- Modify: `src/App.tsx`
- Modify: `src/index.css`

- [ ] **Step 1: Reescrever `src/App.tsx`**

```tsx
import { useState } from 'react'
import { SearchBar } from './components/SearchBar'
import { PokemonCard } from './components/PokemonCard'
import { usePokemon } from './hooks/usePokemon'

export default function App() {
  const [query, setQuery] = useState('')
  const { data, erro, carregando } = usePokemon(query)

  return (
    <div className="container">
      <h1 className="titulo">Pokédex</h1>

      <SearchBar onBuscar={setQuery} />

      {carregando && <p className="mensagem">carregando...</p>}
      {erro && <p className="mensagem erro">{erro}</p>}
      {data && <PokemonCard pokemon={data} />}
    </div>
  )
}
```

- [ ] **Step 2: Reescrever `src/index.css`**

```css
*, *::before, *::after {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

body {
  background-color: #f5f5f5;
  color: #1a1a1a;
  font-family: system-ui, -apple-system, sans-serif;
  min-height: 100vh;
}

.container {
  max-width: 480px;
  margin: 48px auto;
  padding: 0 16px;
  display: flex;
  flex-direction: column;
  gap: 24px;
}

.titulo {
  font-size: 1.5rem;
  font-weight: 700;
  color: #1a1a1a;
}

.search-form {
  display: flex;
  gap: 8px;
}

.search-input {
  flex: 1;
  padding: 10px 12px;
  border: 1px solid #e5e7eb;
  border-radius: 6px;
  font-size: 0.95rem;
  background: #fff;
  color: #1a1a1a;
  outline: none;
}

.search-input:focus {
  border-color: #2563eb;
}

.search-btn {
  padding: 10px 16px;
  background-color: #2563eb;
  color: #fff;
  border: none;
  border-radius: 6px;
  font-size: 0.95rem;
  cursor: pointer;
}

.search-btn:hover {
  background-color: #1d4ed8;
}

.mensagem {
  font-size: 0.9rem;
  color: #6b7280;
}

.mensagem.erro {
  color: #dc2626;
}

.pokemon-card {
  background: #fff;
  border: 1px solid #e5e7eb;
  border-radius: 10px;
  padding: 24px;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 12px;
  box-shadow: 0 1px 4px rgba(0, 0, 0, 0.06);
}

.pokemon-sprite {
  width: 120px;
  height: 120px;
  image-rendering: pixelated;
}

.pokemon-nome {
  font-size: 1.25rem;
  font-weight: 700;
  text-transform: capitalize;
  color: #1a1a1a;
}

.pokemon-tipos {
  display: flex;
  gap: 8px;
}

.pokemon-stats {
  display: flex;
  gap: 24px;
  font-size: 0.85rem;
  color: #6b7280;
}
```

- [ ] **Step 3: Testar no navegador**

```bash
npm run dev
```

Verificar:
- Buscar `pikachu` → aparece card com sprite, nome, tipo electric (badge amarelo), altura e peso
- Buscar `charmander` → badge fire (laranja)
- Buscar `aaaaa` → mensagem de erro
- Buscar string vazia → não faz nada

- [ ] **Step 4: Commit final**

```bash
git add src/App.tsx src/index.css
git commit -m "feat: pokedex completa"
```

---

### Task 8: TypeScript sem erros

**Files:**
- Modify: nenhum (só verificação)

- [ ] **Step 1: Checar tipos**

```bash
npm run build
```

Esperado: build sem erros de TypeScript. Se houver erros, corrigi-los antes de prosseguir.
